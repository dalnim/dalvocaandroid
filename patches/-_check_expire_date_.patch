Index: app/src/main/java/com/dalread/activity/SplashActivity.java
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
--- app/src/main/java/com/dalread/activity/SplashActivity.java	(revision 5e98451d195b41fa4768e6138758c30abec57584)
+++ app/src/main/java/com/dalread/activity/SplashActivity.java	(date 1601707140780)
@@ -21,6 +21,7 @@
 import com.dalread.util.Utils;
 import com.dalread.util.Voca;
 
+import java.util.Calendar;
 import java.util.Set;
 
 /**
@@ -51,6 +52,22 @@
     public void initView() {
         setContentView(R.layout.activity_splash);
         Voca.removeAllNotification(this);
+
+        Calendar calendar = Calendar.getInstance();
+        calendar.set(Calendar.YEAR, 2020);
+        calendar.set(Calendar.MONTH, Calendar.DECEMBER);
+        calendar.set(Calendar.DAY_OF_MONTH, 25); // 1 - 31
+        calendar.set(Calendar.HOUR_OF_DAY, 0); // 0 - 23
+        calendar.set(Calendar.MINUTE, 0);
+        calendar.set(Calendar.SECOND, 0);
+        calendar.set(Calendar.MILLISECOND, 0);
+        long expireDate = calendar.getTimeInMillis();
+        long now = System.currentTimeMillis();
+        if (now >= expireDate) {
+            Utils.showToast(this, "This version is not available anymore!");
+            return;
+        }
+
         if (BuildConfig.FLAVOR.equals(EnumFlavor.VOCA.getName())) {
             if (Constant.DEBUG) {
                 Bundle bundle = getIntent().getExtras();
