<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="120dp"
    android:layout_marginHorizontal="10dp"
    android:layout_marginVertical="3dp"
    android:background="@color/backgroundCellItemColor"
    android:orientation="vertical"
    app:cardBackgroundColor="@color/backgroundCellItemColor"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:contentPadding="2dp">

    <LinearLayout
        android:id="@+id/llItem"
        style="@style/Match"
        android:background="@color/multiPlayerBackgroundLightBlackColor"
        android:orientation="vertical"
        android:textColor="@color/textPrimaryWhiteColor">

        <TextView
            android:id="@+id/tvTitle"
            style="@style/TextViewFontMedium"
            android:layout_width="match_parent"
            android:background="@color/multiPlayerBackgroundBlackColor"
            android:gravity="center"
            android:paddingLeft="6dp"
            android:paddingRight="6dp"
            android:text="@string/tv_seasons"
            android:textColor="@color/textPrimaryWhiteColor"
            android:textSize="@dimen/font_value" />

        <LinearLayout
            style="@style/Match"
            android:background="@color/multiPlayerBackgroundBlackColor"
            android:orientation="horizontal"
            android:textColor="@color/textPrimaryWhiteColor">
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvVideoSeasonList"
                android:layout_height="wrap_content"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:background="@color/multiPlayerBackgroundBlackColor"
                android:layout_gravity="center_vertical"
                android:clipToPadding="false"
                android:nestedScrollingEnabled="false"
                android:paddingHorizontal="8dp"
                android:visibility="gone"
                tools:itemCount="1"
                tools:listitem="@layout/item_player_stored_video_screen"
                tools:visibility="visible" />
            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/ivEdit"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_alignParentEnd="true"
                android:padding="15dp"
                android:layout_gravity="center"
                android:src="@drawable/baseline_close_white_24"
                android:tint="@color/textPrimaryWhiteColor" />
        </LinearLayout>
    </LinearLayout>

</androidx.cardview.widget.CardView>