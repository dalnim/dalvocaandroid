<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_header_view"
    style="@style/Match"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/csHomeSideMenuTopAvatarRectangle"
        android:layout_width="match_parent"
        android:layout_height="126dp"
        android:background="@color/home_side_menu_top_avatar_rectangle"
        android:orientation="horizontal">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/nav_iv_avatar"
            android:layout_width="75dp"
            android:layout_height="75dp"
            android:layout_marginStart="16dp"
            android:src="@drawable/ic_no_avatar_2"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/nav_tv_name"
            style="@style/TextView_Left_Navigation_UserName"
            android:layout_width="0dp"
            android:layout_marginStart="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/nav_iv_avatar"
            app:layout_constraintTop_toTopOf="@+id/nav_iv_avatar"
            tools:text="User Name" />

        <TextView
            android:id="@+id/nav_tv_point"
            style="@style/TextView_Value"
            android:layout_width="0dp"
            android:layout_marginStart="16dp"
            android:textColor="@color/textPrimaryWhiteColor"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/nav_iv_avatar"
            app:layout_constraintTop_toBottomOf="@+id/nav_tv_name"
            tools:text="point" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/listMenu"
        tools:listitem="@layout/item_menu"
        android:background="@color/home_side_menu_item_background"
        style="@style/Match" />
</LinearLayout>
